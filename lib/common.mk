MAKEFLAGS += --no-builtin-rules
MAKEFLAGS += --no-builtin-variables
MAKEFLAGS += --no-print-directory

ifeq (${VERBOSE},0)
 MAKEFLAGS += --silent
 QUIET := >/dev/null
endif

ifneq ($(realpath ${CONFIG}),)
 $(call load-variables)
endif

ALL_TARGETS += shell
MANUAL_TARGETS += shell

AUTO_TARGETS := $(filter-out ${MANUAL_TARGETS},${ALL_TARGETS})

.PHONY: all ${ALL_TARGETS}
all: ${AUTO_TARGETS}

SHELL := /bin/bash

export PROJECT := $(notdir ${REPO_DIR})

DEFAULT_BB_EXPORT_VARIABLES := REPO_DIR BUILD_DIR VERBOSE
DEFAULT_BB_EXPORT_VARIABLES += DL_DIR SSTATE_DIR DISTRO MACHINE

DEFAULT_DOCKER_EXPORT_VARIABLES := REPO_DIR BUILD_DIR VERBOSE
DEFAULT_DOCKER_EXPORT_VARIABLES += OE_INIT_BUILD_ENV
DEFAULT_DOCKER_EXPORT_VARIABLES += ${DEFAULT_BB_EXPORT_VARIABLES}
DEFAULT_DOCKER_EXPORT_VARIABLES += BB_EXPORT_VARIABLES ${BB_EXPORT_VARIABLES}
DEFAULT_DOCKER_EXPORT_VARIABLES += BB_LAYERS
